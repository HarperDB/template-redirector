import { Redirect } from './util/redirect.js';
import { CheckRedirect } from './util/check_redirect.js';
//import { RedirectMetrics } from './util/redirect_metrics.js';

export const redirect = Redirect;
export const checkredirect = CheckRedirect;
//export const redirectmetrics = RedirectMetrics;
